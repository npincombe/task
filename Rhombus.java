
public class Rhombus implements Geometry {
    private int x, y, height, width;

    public void addToDevice(Device device) {
        //draw the shape using drawPolygon and specifying 4 points
    }
}
